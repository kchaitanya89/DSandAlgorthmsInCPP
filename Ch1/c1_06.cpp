#include <iostream>
#include <cstdlib>
#include <string>
#include <vector>

using namespace std;

int fact(int n)
{
    if (n == 1)
        return 1;
    return n * fact(n - 1);
}
int main(int argc, char const *argv[])
{
    char ch[] = {'a', 'b', 'c', 'd', 'e', 'f'};

    cout << (6 >> 6);
    return EXIT_SUCCESS;
}